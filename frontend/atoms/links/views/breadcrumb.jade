include default

mixin atom-breadcrumb
  if attributes.class
    - attributes.class="atom-link-breadcrumb " + attributes.class
  else
    - attributes.class="atom-link-breadcrumb"
  +atom-link(attributes)
    if block
      block
